import React from "react";
import Navbar from "react-bootstrap/Navbar";

// simple Navbar component
const Nav = ({}) => (
  <Navbar bg="dark">
    <h3 className="text-warning m-2">Outlast Bookstore</h3>
  </Navbar>
);

export default Nav;
